# SKIDL 설계

이 폴더는 Python SKIDL 기반 PCB 설계 자동화 코드를 관리한다.

## 기본 방향

- `main.py`는 실행 진입점으로 둔다.
- 실제 회로 구성은 `robot_dog_pcb` 패키지의 객체들이 담당한다.
- 전원부, MCU, 서보 커넥터, 확장 포트는 독립 블록으로 나눈다.
- 초기 구현에서는 객체 구조와 12개 서보 채널 구성을 먼저 검증한다.
